import PlayerDetails from "../Components/PlayerDetails";

function Show() {
  return (
    <div className="Show">
      <section>
        <PlayerDetails />
      </section>
    </div>
  );
}

export default Show;